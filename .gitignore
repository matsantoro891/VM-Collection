# Local secrets and machine-specific overrides
.env
.env.*
!.env.example
config.local.js

# Dependencies (vendor bundle is committed; node_modules is not required at runtime)
node_modules/

# OS / editor
.DS_Store
Thumbs.db
*.log
